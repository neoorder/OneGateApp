<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:og="http://schemas.neoorder.org/onegate/controls"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="NeoOrder.OneGate.Pages.DAppsPage"
             Title="{x:Static og:Strings.Apps}"
             BindingContext="{Binding Source={RelativeSource Self}}">
    <ContentPage.Resources>
        <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        <og:LocalizerConverter x:Key="LocalizerConverter" />
    </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem Order="Primary" IconImageSource="{FontImageSource &#xe69a;, FontFamily=Icons, Color={toolkit:AppThemeResource Primary}}" Command="{x:Static og:Commands.ScanQRCode}" />
    </ContentPage.ToolbarItems>
    <Shell.SearchHandler>
        <og:DAppSearchHandler SearchBoxVisibility="Collapsible" ShowsResults="True" DApps="{Binding DAppsRegular}">
            <og:DAppSearchHandler.ItemTemplate>
                <DataTemplate x:DataType="og:DApp">
                    <Grid ColumnDefinitions="auto,*" ColumnSpacing="8" Margin="{OnPlatform iOS='20,0', MacCatalyst='20,0', Default='5'}">
                        <Border StrokeShape="RoundRectangle 8" Padding="4" WidthRequest="30" HeightRequest="30" VerticalOptions="Center">
                            <Image WidthRequest="22" HeightRequest="22" Aspect="AspectFit">
                                <Image.Source>
                                    <UriImageSource Uri="{Binding IconUrl}" CacheValidity="30:00:00:00" />
                                </Image.Source>
                            </Image>
                        </Border>
                        <Label Grid.Column="1" Text="{Binding Name, Converter={StaticResource LocalizerConverter}}" VerticalOptions="Center" />
                    </Grid>
                </DataTemplate>
            </og:DAppSearchHandler.ItemTemplate>
        </og:DAppSearchHandler>
    </Shell.SearchHandler>
    <ScrollView Margin="20,8">
        <VerticalStackLayout Spacing="14">
            <og:TabBar x:Name="tabbarFavoriteOrRecent" Spacing="4" IsVisible="{Binding HasFavoriteOrRecent}" SelectedTabChanged="FavoriteOrRecent_SelectedTabChanged">
                <og:TabBar.Tabs>
                    <x:Array Type="{x:Type x:String}">
                        <x:Static Member="og:Strings.Recent" />
                        <x:Static Member="og:Strings.Favorite" />
                    </x:Array>
                </og:TabBar.Tabs>
            </og:TabBar>
            <CollectionView ItemsSource="{Binding DAppsFavoriteOrRecent}" HeightRequest="104" IsVisible="{Binding HasFavoriteOrRecent}">
                <CollectionView.EmptyView>
                    <Border StyleClass="Card" HeightRequest="88">
                        <Grid>
                            <Label IsVisible="{Binding LoadingService.IsLoading}" HorizontalOptions="Center" VerticalOptions="Center">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{x:Static og:Strings.Loading}" />
                                        <Span Text="…" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            <Label StyleClass="Secondary" Text="{x:Static og:Strings.DefaultEmptyViewMessage}" IsVisible="{Binding LoadingService.IsLoading, Converter={StaticResource InvertedBoolConverter}}" HorizontalOptions="Center" VerticalOptions="Center" />
                        </Grid>
                    </Border>
                </CollectionView.EmptyView>
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="8" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="og:DApp">
                        <Border StyleClass="Card" WidthRequest="88" Padding="8">
                            <VerticalStackLayout Spacing="6">
                                <Border StrokeShape="RoundRectangle 12" Padding="7" HorizontalOptions="Center">
                                    <Image WidthRequest="42" HeightRequest="42" Aspect="AspectFit">
                                        <Image.Source>
                                            <UriImageSource Uri="{Binding IconUrl}" CacheValidity="30:00:00:00" />
                                        </Image.Source>
                                    </Image>
                                </Border>
                                <Label Text="{Binding Name, Converter={StaticResource LocalizerConverter}}" FontAttributes="Bold" FontSize="12" MaxLines="1" LineBreakMode="TailTruncation" HorizontalOptions="Fill" HorizontalTextAlignment="Center" />
                                <VerticalStackLayout.GestureRecognizers>
                                    <TapGestureRecognizer Command="{x:Static og:Commands.LaunchDApp}" CommandParameter="{Binding .}" />
                                </VerticalStackLayout.GestureRecognizers>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <og:TabBar Tabs="{Binding DAppCategories}" Spacing="4" SelectedTabChanged="OnCategoryChanged" />
            <CollectionView ItemsSource="{Binding DAppsFiltered}" MinimumHeightRequest="20">
                <CollectionView.EmptyView>
                    <Border StyleClass="Card">
                        <Grid>
                            <Label IsVisible="{Binding LoadingService.IsLoading}" HorizontalOptions="Center">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{x:Static og:Strings.Loading}" />
                                        <Span Text="…" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            <Label StyleClass="Secondary" Text="{x:Static og:Strings.DefaultEmptyViewMessage}" IsVisible="{Binding LoadingService.IsLoading, Converter={StaticResource InvertedBoolConverter}}" HorizontalOptions="Center" />
                        </Grid>
                    </Border>
                </CollectionView.EmptyView>
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="6" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="og:DApp">
                        <Border StyleClass="Card" Padding="12">
                            <Grid ColumnDefinitions="auto,*,auto" ColumnSpacing="12">
                                <Border StrokeShape="RoundRectangle 12" Padding="7" WidthRequest="52" HeightRequest="52" VerticalOptions="Center">
                                    <Image WidthRequest="38" HeightRequest="38" Aspect="AspectFit">
                                        <Image.Source>
                                            <UriImageSource Uri="{Binding IconUrl}" CacheValidity="30:00:00:00" />
                                        </Image.Source>
                                    </Image>
                                </Border>
                                <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Center">
                                    <Grid ColumnDefinitions="*,auto" ColumnSpacing="8">
                                        <Label Text="{Binding Name, Converter={StaticResource LocalizerConverter}}" FontAttributes="Bold" FontSize="16" LineBreakMode="TailTruncation" VerticalOptions="Center" />
                                        <Border Grid.Column="1" StrokeShape="RoundRectangle 4" BackgroundColor="{toolkit:AppThemeResource BrandMuted}" Padding="6,2" VerticalOptions="Center" IsVisible="{Binding IsOneGateVault}">
                                            <Label Text="{x:Static og:Strings.Official}" TextColor="{toolkit:AppThemeResource Success}" FontSize="10" FontAttributes="Bold" />
                                        </Border>
                                    </Grid>
                                    <Label StyleClass="Secondary" Text="{Binding Description, Converter={StaticResource LocalizerConverter}}" FontSize="12" MaxLines="2" LineBreakMode="TailTruncation" />
                                </VerticalStackLayout>
                                <Button Grid.Column="2" StyleClass="Icon, IconSecondary" Text="&#xe605;" FontSize="20" Padding="8" WidthRequest="40" HeightRequest="40" VerticalOptions="Center" Clicked="OnDetailsClicked" CommandParameter="{Binding .}" />
                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer Command="{x:Static og:Commands.LaunchDApp}" CommandParameter="{Binding .}" />
                                </Grid.GestureRecognizers>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
