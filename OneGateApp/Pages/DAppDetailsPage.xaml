<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:og="http://schemas.neoorder.org/onegate/controls"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="NeoOrder.OneGate.Pages.DAppDetailsPage"
             x:DataType="og:DApp"
             Title="{x:Static og:Strings.AppDetails}"
             Shell.TabBarIsVisible="False">
    <ContentPage.Resources>
        <og:StringSelectConverter x:Key="StringSelectConverter" />
        <toolkit:IsListNotNullOrEmptyConverter x:Key="IsListNotNullOrEmptyConverter" />
        <toolkit:IsStringNotNullOrEmptyConverter x:Key="IsStringNotNullOrEmptyConverter" />
        <og:LocalizerConverter x:Key="LocalizerConverter" />
        <og:LanguageConverter x:Key="LanguageConverter" />
    </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem Order="Primary" Clicked="OnFavoriteClicked" Command="{x:Static og:Commands.AddToFavorites}" CommandParameter="{Binding .}">
            <ToolbarItem.IconImageSource>
                <FontImageSource Glyph="{Binding IsFavorite, Converter={StaticResource StringSelectConverter}, ConverterParameter=&#xe83a;&#xe839;, Source={RelativeSource AncestorType={x:Type og:DAppDetailsPage}}}" FontFamily="Icons" Color="Gold" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
        <ToolbarItem x:Name="addToHomeScreenButton" Order="Primary" IconImageSource="{FontImageSource &#xe612;, FontFamily=Icons, Color={toolkit:AppThemeResource Primary}}" Command="{x:Static og:Commands.AddToHomeScreen}" CommandParameter="{Binding .}" />
        <ToolbarItem Order="Primary" IconImageSource="{FontImageSource &#xe68b;, FontFamily=Icons, Color={toolkit:AppThemeResource Primary}}" Command="{x:Static og:Commands.Share}" CommandParameter="{Binding .}" />
    </ContentPage.ToolbarItems>
    <ScrollView>
        <VerticalStackLayout Margin="20" Spacing="20">
            <Grid ColumnDefinitions="auto,*" ColumnSpacing="20">
                <Border StrokeShape="RoundRectangle 10">
                    <Image WidthRequest="64" HeightRequest="64" Aspect="AspectFit">
                        <Image.Source>
                            <UriImageSource Uri="{Binding IconUrl}" CacheValidity="30:00:00:00" />
                        </Image.Source>
                    </Image>
                </Border>
                <VerticalStackLayout Grid.Column="1" Spacing="5">
                    <Label Text="{Binding Name, Converter={StaticResource LocalizerConverter}}" FontAttributes="Bold" FontSize="22" />
                    <Label StyleClass="Link" Text="{Binding Developer}" FontAttributes="Bold">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{x:Static og:Commands.OpenUrl}" CommandParameter="{Binding Website}" />
                        </Label.GestureRecognizers>
                    </Label>
                </VerticalStackLayout>
            </Grid>
            <Button Text="{x:Static og:Strings.Open}" Command="{x:Static og:Commands.LaunchDApp}" CommandParameter="{Binding .}" />
            <Border StrokeShape="RoundRectangle 10" BackgroundColor="{toolkit:AppThemeResource Panel}" Padding="16" IsVisible="{Binding IsOneGateVault}">
                <VerticalStackLayout Spacing="12">
                    <Grid ColumnDefinitions="*,auto" ColumnSpacing="10">
                        <Label Text="{x:Static og:Strings.OneGateVaultOfficial}" FontAttributes="Bold" FontSize="16" LineBreakMode="TailTruncation" VerticalOptions="Center" />
                        <Border Grid.Column="1" StrokeShape="RoundRectangle 4" BackgroundColor="{toolkit:AppThemeResource BrandMuted}" Padding="8,3" VerticalOptions="Center">
                            <Label Text="{x:Static og:Strings.Official}" TextColor="{toolkit:AppThemeResource Success}" FontSize="11" FontAttributes="Bold" />
                        </Border>
                    </Grid>
                    <Label StyleClass="Secondary" Text="{x:Static og:Strings.OneGateVaultOfficialText}" FontSize="12" LineBreakMode="WordWrap" />
                    <Grid ColumnDefinitions="*,auto" ColumnSpacing="10">
                        <Label StyleClass="Secondary" Text="{x:Static og:Strings.Status}" FontSize="12" VerticalOptions="Center" />
                        <Label Grid.Column="1" Text="{Binding OneGateVaultStatusText}" TextColor="{toolkit:AppThemeResource Success}" FontSize="12" FontAttributes="Bold" VerticalOptions="Center" />
                    </Grid>
                    <VerticalStackLayout Spacing="4">
                        <Label Text="{x:Static og:Strings.OneGateVaultRules}" FontAttributes="Bold" FontSize="13" />
                        <Label StyleClass="Secondary" Text="{x:Static og:Strings.OneGateVaultRulesText}" FontSize="12" LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                    <VerticalStackLayout Spacing="4">
                        <Label Text="{x:Static og:Strings.OneGateVaultHistory}" FontAttributes="Bold" FontSize="13" />
                        <Label StyleClass="Secondary" Text="{x:Static og:Strings.OneGateVaultHistoryText}" FontSize="12" LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>
            <Label Text="{x:Static og:Strings.GameType}" FontAttributes="Bold" IsVisible="{Binding GameType, Converter={StaticResource IsStringNotNullOrEmptyConverter}}" />
            <Label Text="{Binding GameTypeDisplayName}" Margin="0,-10,0,0" IsVisible="{Binding GameType, Converter={StaticResource IsStringNotNullOrEmptyConverter}}" />
            <Label Text="{x:Static og:Strings.Preview}" FontAttributes="Bold" IsVisible="{Binding Previews, Converter={StaticResource IsListNotNullOrEmptyConverter}}" />
            <CollectionView ItemsSource="{Binding Previews}" IsVisible="{Binding Previews, Converter={StaticResource IsListNotNullOrEmptyConverter}}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="10" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="{x:Type x:String}">
                        <Image WidthRequest="100">
                            <Image.Source>
                                <UriImageSource Uri="{Binding .}" CacheValidity="30:00:00:00" />
                            </Image.Source>
                            <Image.Behaviors>
                                <og:FullScreenBehavior />
                            </Image.Behaviors>
                        </Image>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Label Text="{x:Static og:Strings.Introduction}" FontAttributes="Bold" />
            <Label Text="{Binding Description, Converter={StaticResource LocalizerConverter}}" Margin="0,-10,0,0" />
            <Label Text="{x:Static og:Strings.SupportedLanguages}" FontAttributes="Bold" />
            <Label Text="{Binding Languages, Converter={StaticResource LanguageConverter}}" Margin="0,-10,0,0" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
